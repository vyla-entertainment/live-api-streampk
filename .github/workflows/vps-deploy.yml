name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/live

            git fetch origin main
            git reset --hard origin/main
            git clean -fd

            npm install

            chown -R www-data:www-data /var/www/live
            chmod -R 755 /var/www/live

            pm2 restart live || pm2 start server.js --name live

            pm2 save