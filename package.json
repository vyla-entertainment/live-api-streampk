{
  "name": "streamed-pk-hls-stream-resolver",
  "description": "Node.js resolver for streamed.pk and embed.st HLS streams: GOAT protobuf /fetch + lock.wasm decrypt, golf third-party embed chain, curl relay for CDN playlists and MPEG-TS segments.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "big-integer": "^1.6.52",
    "express": "^4.21.2",
    "happy-dom": "^20.0.10"
  }
}
