const UPSTREAM = 'https://streamed.pk/api';
const CACHE_TTL = 30 * 1000;
const UPSTREAM_TIMEOUT = 8000;
const EMBED_DOMAIN = 'https://embed.st';

module.exports = {
    UPSTREAM,
    CACHE_TTL,
    UPSTREAM_TIMEOUT,
    EMBED_DOMAIN
};